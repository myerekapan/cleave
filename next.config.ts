import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Gmail API calls go directly from client — no need to proxy
  // Fonts loaded via CSS @import from Google Fonts
};

export default nextConfig;
